{
  "name": "@storage/plugin-groups-list",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run package-install && lerna run build --stream",
    "package-install": "lerna clean -y && npm run bootstrap",
    "publish": "npm run build && lerna publish",
    "bootstrap": "lerna bootstrap",
    "initialize": "node tools/bootstrap.js",
    "clean": "lerna clean -y"
  },
  "devDependencies": {
    "typescript": "^4.0.2",
    "lerna": "^6.0.3"
  },
  "dependencies": {
    "chalk": "^5.0.1",
    "inquirer": "^9.1.4",
    "shelljs": "^0.8.5"
  }
}
